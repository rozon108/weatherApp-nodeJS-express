{
  "name": "weatherapp",
  "version": "1.0.0",
  "description": "Weather app via node js express, using weather.org api. We will also use https module",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Rojan",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "express": "^4.18.1"
  }
}
